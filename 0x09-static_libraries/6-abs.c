#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an intege
 *
 * @n: the int to check
 *
 * Return: the absolute value of n
 *
 */

int _abs(int n)

{
	return (abs(n));
}
